# Interframe does not currently need any access transformers — every field/method it touches
# (RenderSystem, Camera, LevelRenderer, Window) is already public. Kept as an empty file because
# build.gradle points `minecraft.accessTransformer` at this path; add entries here only if a future
# OptiFine/Iris compat shim needs to reach a private/protected member directly instead of reflectively.
